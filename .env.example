
# ============================================================================
# LLM Configuration
# ============================================================================

# Local Gemini Proxy Configuration (推薦使用)
PROXY_BASE_URL=http://localhost:8000/openai/v1
PROXY_API_KEY=sk-mysecrettoken123
PROXY_MODEL_NAME=gemini-1.5-flash

# External APIs (if used directly)
GEMINI_API_KEY=your_gemini_api_key
OPENAI_API_KEY=your_openai_api_key

# ============================================================================
# Meilisearch Configuration (新架構)
# ============================================================================

# Meilisearch 服務位址
MEILISEARCH_HOST=http://localhost:7700

# Meilisearch API Key (與 Docker 啟動時的 MEILI_MASTER_KEY 相同)
MEILISEARCH_API_KEY=masterKey

# ============================================================================
# Ollama Configuration (Embedding 服務)
# ============================================================================

# Ollama 服務位址 (用於生成 embeddings)
OLLAMA_HOST=http://localhost:11434

# ============================================================================
# Legacy Configuration (已棄用 - 僅供參考)
# ============================================================================

# Qdrant (已由 Meilisearch 取代)
# QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=

# ============================================================================
# 使用說明
# ============================================================================
#
# 1. 複製此檔案為 .env
#    cp .env.example .env
#
# 2. 啟動 Meilisearch 服務
#    docker run -d \
#      --name meilisearch \
#      -p 7700:7700 \
#      -e MEILI_MASTER_KEY=masterKey \
#      -v $(pwd)/meili_data:/meili_data \
#      getmeili/meilisearch:v1.11
#
# 3. 確認服務運行
#    curl http://localhost:7700/health
#

